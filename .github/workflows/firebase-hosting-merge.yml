# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
env:
  REACT_APP_MAIL_URL: ${{ secrets.REACT_APP_MAIL_URL }}
  REACT_APP_MAIL_API_KEY: ${{ secrets.REACT_APP_MAIL_API_KEY }}
  REACT_APP_ANALYTICS_API_KEY: ${{ secrets.REACT_APP_ANALYTICS_API_KEY }}
  REACT_APP_ANALYTICS_AUTH_DOMAIN: ${{ secrets.REACT_APP_ANALYTICS_AUTH_DOMAIN }}
  REACT_APP_ANALYTICS_PROJECT_ID: ${{ secrets.REACT_APP_ANALYTICS_PROJECT_ID }}
  REACT_APP_ANALYTICS_STORAGE_BUCKET: ${{ secrets.REACT_APP_ANALYTICS_STORAGE_BUCKET }}
  REACT_APP_ANALYTICS_MESSAGING_SENDER_ID: ${{ secrets.REACT_APP_ANALYTICS_MESSAGING_SENDER_ID }}
  REACT_APP_ANALYTICS_APP_ID: ${{ secrets.REACT_APP_ANALYTICS_APP_ID }}
  REACT_APP_ANALYTICS_MEASUREMENT_ID: ${{ secrets.REACT_APP_ANALYTICS_MEASUREMENT_ID }}
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_MYWEBSITE_C93BE }}
          channelId: live
          projectId: mywebsite-c93be
